"""Настройки проекта «Муха играет в Chicken Gun».

Всё, что можно крутить, лежит здесь. После правок в разделе «Данные»
нужно заново запустить prepare_data.py, остальное подхватывается сразу.
"""
import json
from pathlib import Path

ROOT = Path(__file__).parent
DATA = ROOT / "data"
DATA.mkdir(exist_ok=True)

# ---------------------------------------------------------------- Данные ----
# Какие связи брать (Codex -> Connections):
#   "filtered"   — только пары нейронов, связанные 5+ синапсами;
#   "unfiltered" — все связи, даже из 1–4 синапсов.
# У каждого варианта свой файл мозга: после смены запусти prepare_data.py один раз.
CONNECTIONS = "unfiltered"
CONNECTIONS_FILES = {
    "filtered": Path(r"C:\Users\maxim\Downloads\connections_princeton.csv\connections_princeton.csv"),
    "unfiltered": Path(r"C:\Users\maxim\Downloads\connections_princeton_no_threshold.csv.gz"),
}
# setup.bat (find_connections.py) находит скачанные файлы сам и пишет пути сюда
CONNECTIONS_JSON = DATA / "connections.json"
if CONNECTIONS_JSON.exists():
    CONNECTIONS_FILES.update({k: Path(v) for k, v in
                              json.loads(CONNECTIONS_JSON.read_text(encoding="utf-8")).items()})
CONNECTIONS_CSV = CONNECTIONS_FILES[CONNECTIONS]
# Выбросить пары нейронов, связанные слабее, чем столько синапсов.
# Для "unfiltered" можно взять середину: 1 — все 19.8 млн пар (медленно),
# 2 — 10.0 млн, 3 — 6.6 млн, 5 — те же 3.7 млн, что и в "filtered".
MIN_SYNAPSES = 1
# Типы и координаты нейронов (github.com/flyconnectome/flywire_annotations)
ANNOTATIONS_TSV = DATA / "neuron_annotations.tsv"
# Готовый мозг, который собирает prepare_data.py
BRAIN_FILE = DATA / f"brain_{CONNECTIONS}.npz"

# Эти нейромедиаторы тормозят, остальные возбуждают (как в Shiu et al., 2024)
INHIBITORY_NT = {"GABA", "GLUT"}

# Какие зрительные нейроны получают сигнал прямо из картинки и какой.
# Фоторецепторы видят свет. Дальше в настоящей мухе работают плавные
# «аналоговые» потенциалы, которые модель со спайками передать не может,
# поэтому раннее зрение считаем из кадра и подаём в нужные нейроны:
#   on / off     — стало светлее / темнее;
#   ftb / btf    — движение спереди назад / сзади вперёд;
#   up / down    — движение вверх / вниз;
#   edge         — контуры предметов и силуэты (есть и у неподвижных вещей).
EYE_INPUTS = {
    "R1-6": "light",
    "Mi1": "on", "Tm3": "on",
    "Tm1": "off", "Tm2": "off", "Tm9": "off",
    "T4a": "ftb", "T4b": "btf", "T4c": "up", "T4d": "down",
    "T5a": "ftb", "T5b": "btf", "T5c": "up", "T5d": "down",
    # T2/T3 и Tm20 передают сигнал в лобулу, к детекторам объектов (LC)
    "T2": "edge", "T2a": "edge", "T3": "edge", "Tm20": "edge",
}
# Колончатые нейроны: через них по связям передаётся «куда смотрит» нейрон
COLUMNAR_TYPES = ["L1", "L2", "L3", "L4", "L5", "C2", "C3", "T1",
                  "Mi1", "Mi4", "Mi9", "Tm1", "Tm2", "Tm3", "Tm4", "Tm9", "Tm20",
                  "T4a", "T4b", "T4c", "T4d", "T5a", "T5b", "T5c", "T5d",
                  "T2", "T2a", "T3"]

# ------------------------------------------------------- Модель нейрона ----
# Чем считать мозг: "cuda" (видеокарта), "numba" (все ядра процессора),
# "numpy" (запасной, медленный) или "auto" — первый рабочий из BACKEND_ORDER
BRAIN_BACKEND = "auto"
BACKEND_ORDER = ["cuda", "numba", "numpy"]
NUMBA_PARALLEL = True    # numba: обновлять нейроны на нескольких ядрах
NUMBA_THREADS = 4        # больше потоков на этой задаче только мешают

# Leaky integrate-and-fire, параметры из Shiu et al., 2024 (Nature)
DT_MS = 1.0            # шаг симуляции (0.5 точнее, но вдвое медленнее)
V_REST = -52.0         # мВ
V_RESET = -52.0
V_THRESH = -45.0
TAU_M_MS = 20.0        # мембрана
TAU_SYN_MS = 5.0       # синапс
REFRACTORY_MS = 2.2
DELAY_MS = 1.8         # задержка передачи
W_SYN_MV = 0.275       # вклад одного синапса
# Адаптация: каждый спайк немного «утомляет» нейрон. Без неё мозг
# от сильной картинки уходит в вечную самоподдерживающуюся активность.
ADAPT_MV = 1.0         # 0 = выключить
TAU_ADAPT_MS = 300.0
# Синаптическая депрессия: доля запаса медиатора на один спайк и время
# восстановления. 0 = выключить.
STD_USE = 0.2
STD_TAU_MS = 300.0
# Спонтанная активность: каждый нейрон изредка спайкает сам по себе.
# Мозг сильно её усиливает, поэтому значения крошечные. 0 = муха замирает,
# когда на экране ничего не меняется.
SPONTANEOUS_HZ = 0.1

# ------------------------------------------------------------------ Глаз ----
EYE_GRID = (64, 36)    # до какого разрешения ужимаем кадр (ширина, высота)
MAX_RATE_HZ = 150.0    # максимальная частота фоторецептора
LUM_WEIGHT = 0.15      # фоторецепторы: вклад просто яркости
MOTION_WEIGHT = 4.0    # фоторецепторы: вклад изменения яркости между кадрами
CONTRAST_GAIN = 6.0    # on/off: изменение яркости, дающее максимум = 1/CONTRAST_GAIN
FLOW_FULL_PX = 1.5     # движение: сдвиг (в клетках сетки за кадр) для максимума
EDGE_GAIN = 5.0        # контуры: доля пикселей-границ в клетке, дающая максимум = 1/EDGE_GAIN
EDGE_MAX_HZ = 40.0     # частота нейрона на самом плотном контуре
# Карта контуров чуть дрожит от кадра к кадру, как глаз живой мухи
# (микросаккады): иначе нейроны привыкают к неподвижной картинке
MICROSACCADES = True
# True: каждый глаз растянут на свою половину экрана (больше сигнала).
# False: честная геометрия, экран виден только передней частью глаз.
EYE_STRETCH = True
GAME_HFOV_DEG = 90.0   # углы обзора игры, нужны при EYE_STRETCH = False
GAME_VFOV_DEG = 60.0

# ------------------------------------------------------------------ Игра ----
WINDOW_TITLE = "BlueStacks"
WINDOW_PROCESS = "HD-Player.exe"   # None = искать только по заголовку
# Обычно область игры берётся из окна «BlueStacks Keymap Overlay».
# Если его нет, отрезаем от главного окна заголовок и боковую панель (пиксели).
CROP = {"top": 33, "bottom": 0, "left": 0, "right": 35}
# Области интерфейса, которые муха не видит: (x0, y0, x1, y1) в долях кадра
HUD_MASKS = [
    (0.00, 0.00, 0.29, 0.22),   # здоровье, иконки, чат
    (0.80, 0.00, 1.00, 0.11),   # камера, меню, настройки
    (0.73, 0.15, 1.00, 1.00),   # кнопки справа
    (0.10, 0.55, 0.28, 0.86),   # джойстик
    (0.56, 0.53, 1.00, 1.00),   # автомат в руках
]
HUD_MASKS_DEFAULT = list(HUD_MASKS)
# Зоны можно двигать мышкой в окне Fly view и сохранять клавишей MASKS_SAVE_KEY —
# тогда они лежат здесь и заменяют список выше
MASKS_FILE = DATA / "hud_masks.json"
if MASKS_FILE.exists():
    HUD_MASKS = [tuple(m) for m in json.loads(MASKS_FILE.read_text(encoding="utf-8"))]

BRAIN_MS_PER_FRAME = 100   # сколько миллисекунд мозга считаем на один кадр

# Клавиша -> нейроны: (тип, сторона или None = обе стороны).
# "class:descending" = все нисходящие нейроны сразу.
CONTROLS = {
    # вперёд: мозг возбуждён -> муха идёт (почти все нисходящие нейроны
    # активны при ходьбе). DNp09 (P9) в модели почти молчит.
    "w": [("class:descending", None)],
    "s": [("MDN", None)],                             # moonwalker: отступление
    # взгляд влево/вправо: DNp15 (DNHS1) получает сигнал от HS-клеток,
    # которые чувствуют поворот мира; муха поворачивается вслед за ним
    "v": [("DNp15", "left")],
    "n": [("DNp15", "right")],
    # взгляд вверх/вниз: нисходящие нейроны, различающие движение мира по вертикали
    "c": [("DNp31", None)],
    "b": [("DNg46", None), ("DNp20", None)],
    "a": [("DNa02", "left"), ("DNa01", "left")],      # у мухи поворот, в игре шаг влево
    "d": [("DNa02", "right"), ("DNa01", "right")],    # у мухи поворот, в игре шаг вправо
    # прыжок: Giant Fiber (DNp01) и у настоящей мухи запускает прыжок-взлёт
    "space": [("DNp01", None)],
    # выстрел: DNp70 в модели самый избирательный детектор надвигающегося объекта
    "y": [("DNp70", None)],
    # редкие нейроны, выбраны условно: почти молчат, иногда отвечают на надвигание/поворот
    "r": [("DNpe042", None)],                         # взять предмет
    "x": [("DNb03", None)],                           # меню с пропами
    "z": [("DNge059", None)],                         # вторая камера
}
# Клавиша нажимается, когда её нейроны (сглаженно, за SMOOTH_MS) частят
# сильнее своего обычного уровня (за BASELINE_S секунд) хотя бы на THRESHOLD_HZ.
THRESHOLD_HZ = {"w": 0.3, "s": 3, "v": 6, "n": 6, "c": 6, "b": 4,
                "a": 5, "d": 5, "space": 6, "y": 10, "r": 3, "x": 3, "z": 3}
SMOOTH_MS = 300
BASELINE_S = 5.0
# В парах решает разница: V жмётся, если левые нейроны обгоняют правые
OPPOSITES = [("v", "n"), ("c", "b"), ("a", "d")]
# Отступление важнее ходьбы вперёд (у мух MDN тоже подавляет ходьбу вперёд)
OVERRIDES = {"s": ["w"]}
# Эти клавиши нажимаются коротко, остальные держатся, пока нейроны активны
TAP_KEYS = {"r", "x", "z", "space"}
# Эти клавиши жмутся не чаще, чем раз в столько секунд
COOLDOWN_S = {"space": 1, "r": 2, "x": 20, "z": 15}
# Переключатели: через столько секунд программа жмёт их ещё раз и возвращает
# как было (меню X закрывается само, когда муха выбирает предмет)
TOGGLE_BACK_S = {"z": 4}
# Сколько держать клавишу после одной вспышки активности (иначе курица дёргается)
KEY_HOLD_MS = 300
# Сначала движение, потом камера: пока нажаты клавиши ходьбы, камера ждёт,
# а повороты, которые муха просила за это время, выполняются после ходьбы
LOOK_AFTER_MOVE = True
MOVE_KEYS = {"w", "a", "s", "d"}
LOOK_KEYS = {"v": ("h", -1), "n": ("h", 1), "c": ("v", -1), "b": ("v", 1)}   # ось и направление
LOOK_WAIT_MAX_MS = 1500   # сколько поворота (в мс нажатия) можно накопить за время ходьбы

# Экран смерти: две белые кнопки внизу по центру (доли кадра игры).
# Когда он виден, муха жмёт «Продолжить», пока не возродится.
DEATH_CONTINUE_BOX = (0.40, 0.775, 0.60, 0.835)   # внутри кнопки «Продолжить»
DEATH_MENU_BOX = (0.40, 0.900, 0.60, 0.960)       # внутри кнопки «Меню»
DEATH_CLICK = (0.50, 0.805)                       # куда кликать («Продолжить»)
HEALTH_BAR_BOX = (0.025, 0.020, 0.170, 0.040)     # зелёная полоска здоровья
RESPAWN_CLICK_EVERY_S = 0.7   # как часто кликать
RESPAWN_CLICK_FOR_S = 10      # сколько секунд подряд кликать
RESPAWN_PAUSE_S = 5           # пауза, если не получилось, потом снова

# Меню с пропами (X): муха водит указателем по сетке предметов и кликает по одному.
# Сетка в долях кадра игры: левый верхний угол, размер клетки, сколько клеток.
MENU_GRID = {"x0": 0.0245, "y0": 0.246, "cell_w": 0.0699, "cell_h": 0.1221, "cols": 8, "rows": 5}
MENU_CLOSE_BOX = (0.614, 0.269, 0.642, 0.320)    # красная кнопка закрытия меню
MENU_SEARCH_BOX = (0.611, 0.362, 0.694, 0.418)   # белое поле «Поиск»
# Какое новое нажатие куда двигает указатель: (клетки вправо, клетки вниз)
MENU_MOVES = {"v": (-1, 0), "a": (-1, 0), "n": (1, 0), "d": (1, 0), "c": (0, -1), "b": (0, 1)}
MENU_PICK_S = 6   # если нейрон «взять» (R) молчит, через столько секунд муха выбирает то, что под указателем

# Выбор оружия: Tab открывает меню оружия, цифра на нумпаде выбирает
# (меню закрывается само). За каждое оружие «голосуют» свои нейроны;
# побеждает группа, которая сильнее всех выделилась над своей нормой.
WEAPON_MENU_KEY = "tab"
WEAPON_VOTERS = {
    "num1": [("DNp70", None)],                                  # детектор надвигания
    "num2": [("LC11", None)],                                   # детектор мелких целей
    "num3": [("HSE", None), ("HSN", None), ("HSS", None)],      # чувствуют движение вокруг
    "num4": [("MDN", None)],                                    # отступление
    "num5": [("DNp01", None)],                                  # Giant Fiber: угроза вплотную
}
WEAPON_NAMES = {"num1": "двустволку", "num2": "снайперку", "num3": "автомат",
                "num4": "предмет 4", "num5": "молоток"}
WEAPON_REASONS = {"num1": "что-то надвигается", "num2": "вдали мелкая цель",
                  "num3": "вокруг много движения", "num4": "хочется отступить",
                  "num5": "угроза вплотную"}
WEAPON_Z = 4.0             # во сколько раз всплеск группы больше её обычных колебаний
WEAPON_SMOOTH_MS = 1000    # по какому окну голосуют группы
WEAPON_EVERY_S = 10        # менять оружие не чаще, чем раз в столько секунд
WEAPON_MENU_DELAY_S = 0.5  # пауза между Tab и цифрой

# Переназначение клавиш прямо в окне «Мозг мухи»: клик по строке клавиши
# переключает её на следующий вариант из этого списка (до перезапуска).
# Чтобы закрепить выбор, поправь CONTROLS выше.
REMAP_CHOICES = {
    "w": [[("class:descending", None)], [("DNp09", None)], [("DNb05", None)]],
    "s": [[("MDN", None)], [("DNp31", None)], [("DNp10", None)]],
    "v": [[("DNp15", "left")], [("DNa02", "left")], [("DNa01", "left")],
          [("HSE", "left"), ("HSN", "left"), ("HSS", "left")]],
    "n": [[("DNp15", "right")], [("DNa02", "right")], [("DNa01", "right")],
          [("HSE", "right"), ("HSN", "right"), ("HSS", "right")]],
    "c": [[("DNp31", None)], [("DNp20", None)], [("DNg46", None)]],
    "b": [[("DNg46", None), ("DNp20", None)], [("DNg46", None)], [("DNp22", None)]],
    "a": [[("DNa02", "left"), ("DNa01", "left")], [("DNa01", "left")], [("DNp15", "left")]],
    "d": [[("DNa02", "right"), ("DNa01", "right")], [("DNa01", "right")], [("DNp15", "right")]],
    "space": [[("DNp01", None)], [("DNp70", None)], [("DNp02", None)]],
    "y": [[("DNp70", None)], [("DNp01", None)], [("LC11", None)], [("LPLC2", None)]],
    "r": [[("DNpe042", None)], [("DNpe045", None)], [("DNb03", None)]],
    "x": [[("DNb03", None)], [("DNge119", None)], [("DNg71", None)]],
    "z": [[("DNge059", None)], [("DNa04", None)], [("DNp53", None)]],
}

PAUSE_KEY = "F8"    # старт / пауза
MASKS_SAVE_KEY = "F6"   # сохранить красные зоны
MASKS_RESET_KEY = "F7"  # вернуть обычные красные зоны
BRAIN_VIEW_KEY = "F9"   # показать / спрятать окно «Мозг мухи»
SHOW_BRAIN = True       # показывать его сразу при запуске
QUIT_KEY = "F10"    # выход
